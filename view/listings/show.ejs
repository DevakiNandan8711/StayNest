<% layout("/layout/boilerplate") %>

    <script>
        // Ensure map variables are globally available before map.js module loads
        window.mapToken = "<%= process.env.MAPBOX_TOKEN %>";
        window.coordinates = <% - JSON.stringify(listing.geometry && listing.geometry.coordinates ? listing.geometry.coordinates : [77.5946, 12.9716]) %>;
        window.locationName = "<%= listing.location %>";
    </script>

    <style>
        /* Styling for the show page */
        .show-card {
            border-radius: 1rem !important;
            overflow: hidden;
            border: none;
        }

        .show-img {
            height: 50vh;
            width: 100%;
            object-fit: cover;
            border-radius: 1rem !important;
        }

        /* Fixed Button Container */
        .btns {
            display: flex;
            gap: 1.5rem;
            margin-top: 1rem;
            align-items: center;
        }

        /* Target the link directly and the button inside the form */
        .btns .edit-btn,
        .btns .delete-btn {
            width: 100%;
            /* Use flexible width inside their parent item */
            height: 2.8rem !important;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            border-radius: 0.5rem;
            font-weight: 500;
            margin: 0 !important;
            text-decoration: none;
        }

        /* Fixed width container for desktop */
        .btn-container-item {
            width: 8rem;
        }

        /* Map container styling */
        #map {
            width: 100%;
            height: 400px;
            border-radius: 1rem;
            border: 1px solid #ddd;
            background-color: #f7f7f7;
        }

        @media (max-width: 576px) {
            .show-img {
                height: 35vh;
            }

            .card-body {
                padding: 1rem 0 !important;
            }

            .offset-md-2 {
                margin-left: 0 !important;
            }

            .btns {
                gap: 1rem;
                width: 100%;
            }

            .btn-container-item {
                flex: 1;
                /* Each takes exactly 50% */
                width: auto;
            }
        }
    </style>

    <div class="row mt-3">
        <div class="col-12 col-md-8 offset-md-2">
            <h3 class="mb-3">
                <%= listing.title %>
            </h3>
        </div>

        <div class="col-12 col-md-8 offset-md-2 mb-3">
            <div class="card show-card listing-card">
                <img src="<%= listing.image.url %>" alt="listing_image" class="card-img-top show-img" />
                <div class="card-body">
                    <p class="card-text">
                        Owned by <b>
                            <%= listing.owner.username %>
                        </b>
                    </p>

                    <p class="card-text">
                        <%= listing.description %>
                    </p>

                    <p class="card-text">
                        &#8377; <%= listing.price ? listing.price.toLocaleString("en-IN") : "N/A" %> / night
                    </p>

                    <p class="card-text">
                        <i class="fa-solid fa-location-dot me-2"></i>
                        <%= listing.location %>, <%= listing.country %>
                    </p>
                </div>
            </div>

            <% if(currentUser && listing.owner._id.equals(currentUser._id)) { %>
                <div class="btns mb-4">
                    <div class="btn-container-item">
                        <a href="/listings/<%=listing._id %>/edit" class="btn btn-dark edit-btn">Edit</a>
                    </div>
                    <form method="post" action="/listings/<%=listing._id %>?_method=DELETE" class="btn-container-item">
                        <button class="btn btn-danger delete-btn">Delete</button>
                    </form>
                </div>
                <% } %>

                    <hr />

                    <% if(currentUser) { %>
                        <div class="mb-4">
                            <h4>Leave a review</h4>
                            <form action="/listings/<%=listing.id %>/reviews" method="post" novalidate
                                class="needs-validation">
                                <div class="mb-3 mt-3">
                                    <label for="rating" class="form-label">Rating</label>
                                    <fieldset class="starability-slot">
                                        <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]"
                                            value="0" checked aria-label="No rating." />
                                        <input type="radio" id="first-rate1" name="review[rating]" value="1" />
                                        <label for="first-rate1" title="Terrible">1 star</label>
                                        <input type="radio" id="first-rate2" name="review[rating]" value="2" />
                                        <label for="first-rate2" title="Not good">2 stars</label>
                                        <input type="radio" id="first-rate3" name="review[rating]" value="3" />
                                        <label for="first-rate3" title="Average">3 stars</label>
                                        <input type="radio" id="first-rate4" name="review[rating]" value="4" />
                                        <label for="first-rate4" title="Very good">4 stars</label>
                                        <input type="radio" id="first-rate5" name="review[rating]" value="5" />
                                        <label for="first-rate5" title="Amazing">5 stars</label>
                                    </fieldset>
                                </div>

                                <div class="mb-3 mt-3">
                                    <label for="comment" class="form-label">Comments</label>
                                    <textarea name="review[comment]" id="comment" cols="30" rows="5"
                                        class="form-control" required></textarea>
                                    <div class="invalid-feedback">Please add some comments for the review</div>
                                </div>

                                <button class="btn btn-outline-dark">Submit Review</button>
                            </form>
                        </div>
                        <hr />
                        <% } %>

                            <% if(listing.reviews.length> 0) { %>
                                <div class="row">
                                    <p><b>All Reviews</b></p>
                                    <% for(review of listing.reviews) { %>
                                        <div class="col-12 col-md-6 mb-3">
                                            <div class="card h-100">
                                                <div class="card-body">
                                                    <h5 class="card-title">@<%=review.author.username%>
                                                    </h5>
                                                    <p class="starability-result card-text"
                                                        data-rating="<%=review.rating%>"></p>
                                                    <p class="card-text">
                                                        <%=review.comment%>
                                                    </p>
                                                    <% if(currentUser && review.author._id.equals(currentUser._id)) { %>
                                                        <form class="mb-1" method="post"
                                                            action="/listings/<%=listing._id %>/reviews/<%=review._id %>?_method=DELETE">
                                                            <button class="btn btn-sm btn-dark">Delete</button>
                                                        </form>
                                                        <% } %>
                                                </div>
                                            </div>
                                        </div>
                                        <% } %>
                                </div>
                                <hr />
                                <% } %>

                                    <div class="mb-5">
                                        <h3>Where you'll be</h3>
                                        <div id="map" class="mt-3"></div>
                                    </div>
        </div>
    </div>

    <script type="module" src="/js/map.js"></script>